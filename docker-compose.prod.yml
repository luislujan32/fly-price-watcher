services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    volumes:
      - mongo_data:/data/db

  app:
    build:
      context: .
    env_file:
      - .env.production
    depends_on:
      - mongo
    restart: unless-stopped

volumes:
  mongo_data:
