services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - mongo_data:/data/db

  app:
    build:
      context: .
    env_file:
      - .env
    depends_on:
      - mongo
    restart: "no"

volumes:
  mongo_data:
